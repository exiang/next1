/** @type {import('next').NextConfig} */
// you need to do that each time you make changes on your next.config.js file.
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com']
  }
}

module.exports = nextConfig
